# Verified Extra High directive — 2026-09-01

Status: `COMPLETE_PENDING_REQUIRED_PRO_META_REVIEW`

This file preserves the independently observed ChatGPT surface/session receipt and
the complete response returned in that same visibly selected Extra High conversation.
The response does not self-attest the browser receipt. No merge or deployment authority
is conferred by this record.

## Independent routing receipt

```json
{
  "schemaVersion": 1,
  "requiredSurface": "SIGNED_IN_CHATGPT_CHAT",
  "requiredVisibleMode": "Extra High",
  "browserSurface": "Brave Browser connected extension",
  "accountReceipt": "u-dont-exist.com Pro visible in profile control",
  "conversationUrl": "https://chatgpt.com/c/6a973fd5-9228-83e9-abad-b5f0a5510de2",
  "conversationSessionId": "6a973fd5-9228-83e9-abad-b5f0a5510de2",
  "inputPayloadSha256": "a65c4bcceeabf88b56ed3413c4183ed77f228cd8e5765a36c0ccec80a34b8309",
  "submittedVisiblePayloadSha256": "ac8678637a66ef3748068b9b1ecb346209a6ce5f4dec04e3044b2edf9ec4af5e",
  "responsePayloadSha256": "e8772e5509415121933277881125378ba8a080dbb76bde29778235819e7d5607",
  "submittedMessageReceipt": {
    "userTurnCount": 1,
    "packet1": true,
    "packet2": true,
    "boundHead": true
  },
  "completedResponseReceipt": {
    "assistantTurnCount": 1,
    "stopButtonAbsent": true,
    "directiveId": true,
    "directiveVersion": true
  },
  "visibleModePostResponseReceipt": true
}
```

## Complete Extra High response

I verified the live GitHub state: Human Design PR #23 is still draft at 7156fd61e0e75a56f66024c65357c1256bf8f4f9, and Mission Control PR #52 remains draft at 6ec73802cf9439be7160f9ac2eea58c7bb95e683. The current Action schema does accept client-authored cluster_id, while the backend constructs the frozen prediction dimension—including its cluster—server-side. The current progress schema also describes required_confirmatory_question_count and 100% coverage as a lock gate, which is unsafe while the completeness policy remains unauthorized.

The bounded existing-work scan supports ADAPT + COMPOSE, not a novel provenance framework. in-toto provides immutable subject/predicate binding; SLSA explicitly distinguishes trusted provenance generated by the control plane from tenant-controlled inputs; RATS separates the entity making claims from the verifier that appraises them; and NIST federation likewise treats identity assertions as coming from an identity provider to a relying party rather than from the subject's chosen label. 
NIST Sécurité Informatique
+4
GitHub
+4
SLSA
+4

Edit
VERIFIED REASONING DIRECTIVE

directiveId: XH-MC-HD-AUTHORITY-PROVENANCE-20260901
version: 1.0.0
issuedFor:

u-dont-existDOTcom/universal-dev-architecture PR #52

u-dont-existDOTcom/humandesign PR #23

currentBoundHeads:

missionControlPr52: 6ec73802cf9439be7160f9ac2eea58c7bb95e683

humanDesignPr23: 7156fd61e0e75a56f66024c65357c1256bf8f4f9

humanDesignImplementationParent: 69c2d5796f871fae248f49acfd6778c336d9bf45

productionMainObservedInRoutingPacket: afc0bb82de0e481ae5a5d3453e0bcaf82b2a0286

1. CLAIM-AUTHORITY CANDIDATE VERDICT

verdict: REVISE

The candidate has the correct core diagnosis and should be preserved, not discarded:

distinguish artifact facts from scientific/product/release criteria;

prohibit executor-derived facts from silently becoming acceptance criteria;

register load-bearing claims with provenance;

fail closed on unregistered load-bearing additions;

rerun reconciliation when a load-bearing criterion changes;

independently reproduce load-bearing artifact cardinalities;

do not treat synthetic fixtures as production-cardinality evidence.

Required revisions before canonical adoption:

CA-1 — add an authority ceiling

Every load-bearing claim SHALL carry:

claimKind

currentAuthority

requiredAuthority

authoritySourceRef

subjectRef

verificationState

REASONING_DECISION is not universally sufficient merely because it came from a reasoning chat.

For the Human Design 23-versus-76 completeness decision in this task:

requiredAuthority = OWNER_EXPLICIT

Extra High or Pro may analyze and recommend a criterion, but SHALL NOT convert that recommendation into an owner-authorized completeness rule.

CA-2 — separate derivation from promotion

An ARTIFACT_DERIVED_FACT may be stated as a fact when adequately reproduced.

It may not populate a field classified as:

SCIENTIFIC_CRITERION

PRODUCT_DECISION

RELEASE_CONDITION

OWNER_ACCEPTANCE_CRITERION

without a separate authorized promotion record.

Add:

claimTransition = DERIVED | PROMOTED | REVOKED | SUPERSEDED

A promotion requires a new authority source. Copying the same value into a task contract is not promotion authority.

CA-3 — bind provenance to the actual subject

Claim records SHALL bind to the immutable artifact/directive/source they concern, using commit/hash/version where available.

A registry row that merely says verified: true is insufficient.

Minimum provenance:

source/subject identity;

immutable digest or commit where available;

derivation method for derived facts;

evidence issuer/observer;

verification time/freshness;

applicable directive ID/version;

authority source;

independent reproduction status where required.

CA-4 — bound independent reproduction

Independent reproduction is REQUIRED for artifact-derived facts that materially affect scientific acceptance, owner acceptance, release, or a consequential supervisory verdict.

It is NOT required for every harmless implementation integer or incidental constant.

Independent reproduction proves the fact only. It never supplies the authority to make that fact a criterion.

CA-5 — fail closed on semantic reclassification

A worker SHALL NOT change:

FACT -> SCIENTIFIC_CRITERION

or any equivalent fact-to-policy transition merely by changing field names, copying prose into a directive, or obtaining worker-to-contract GREEN.

Expected failure:

UNAUTHORIZED_CLAIM_PROMOTION

This is additional to:

UNAUTHORIZED_ADDITION

INFERRED_NUMERIC_SCOPE

DERIVATION_UNVERIFIED

DIRECTIVE_SCOPE_EXCEEDED

2. REASONING-SURFACE IDENTITY LAUNDERING DIAGNOSIS

rootCause:
TRUST_DOMAIN_CONFUSION + SELF_ASSERTED_IDENTITY_PROMOTION

The defect is not principally a naming bug.

A worker-controlled namespace—agent name, task name, role name, branch, prompt text, environment variable, or model self-description—was treated as if it were an observation made by the required ChatGPT reasoning surface.

That collapses the claimant and verifier into one trust domain.

The repair SHALL treat ChatGPT surface identity, account identity, visible reasoning mode, conversation/session identity, submission receipt, and response receipt as separate externally evidenced claims.

A worker-controlled name has zero evidentiary weight for all six.

3. MISSION CONTROL REPAIR — REASONING SURFACE ATTESTATION v1

repairId: MC-REASONING-SURFACE-ATTESTATION
version: 1.0.0

Required new machine-readable receipt

Add a versioned template equivalent to:

templates/REASONING-SURFACE-ATTESTATION.json

Required fields:

schemaVersion

reviewRequirementId

requiredSurface

requiredVisibleMode

surfaceReceipt

accountReceipt

visibleModePreSubmissionReceipt

conversationSessionReceipt

submittedMessageReceipt

completedResponseReceipt

visibleModePostResponseReceipt

inputPayloadSha256

responsePayloadSha256

observerType

evidenceSourceType

observedAt

verificationState

mismatchReasons

Each receipt field independently reports:

VERIFIED | MISSING | MISMATCH | UNVERIFIED

Aggregate review identity is VERIFIED_COMPLETE only if every field required by that review requirement passes.

Explicitly invalid evidence sources

The following SHALL NEVER satisfy any reasoning-surface receipt:

Codex agent name;

collaboration-subagent name;

task name;

role label;

branch name;

worktree name;

filesystem path;

process name;

environment variable;

prompt instruction saying "Extra High" or "Pro";

model output claiming its own mode;

packet author identity;

GitHub prose merely asserting a review occurred.

Record attempts as:

SELF_ASSERTED_REASONING_IDENTITY_REJECTED

Same-session binding

Surface/account/mode/submission/response evidence SHALL bind to the same conversation/session.

A receipt from a different or prior conversation cannot be transplanted.

Required replay failure:

REASONING_RECEIPT_SESSION_MISMATCH

Required stale-receipt failure:

REASONING_RECEIPT_REPLAY_REJECTED

Pre/post mode

A visible Extra High mode before submission without a completed-response receipt and post-response mode observation is a partial receipt, not a completed Extra High review.

This response SHALL NOT self-attest its own conversation URL, session ID, submitted-message receipt, completed-response receipt, or post-response mode. Those remain independently recorded by the routing executor as specified in the supplied routing receipt.

Assurance terminology

Do not call this cryptographic platform attestation unless the platform actually supplies cryptographically verifiable evidence.

Current UI/browser observations should be typed as:

OBSERVED_SURFACE_RECEIPT

not:

PLATFORM_SIGNED_ATTESTATION

4. HUMAN DESIGN / ASTROHD PR #23 DIRECTIVE
HD-1 — cluster_id contract

decision:
REMOVE cluster_id FROM CLIENT INPUT

Do not make the interviewer generate, infer, choose, echo, or preserve a trusted cluster_id.

The server is authoritative.

Implementation:

Remove cluster_id from the Action EvidenceInput schema.

Reject client-supplied cluster_id rather than silently trusting it.

When natal-ranking-eligible evidence references question_id, resolve its frozen dimension from the session's immutable PredictionFreeze.

Require exactly one frozen dimension for that question_id.

Obtain cluster_id from that server-side frozen dimension.

Persist the resolved cluster internally where scoring requires it.

Do not expose the cluster merely to make the client echo it back.

Failure states:

zero frozen matches:
FROZEN_QUESTION_BINDING_MISSING

multiple frozen dimension matches:
FROZEN_QUESTION_BINDING_AMBIGUOUS

client attempts to inject cluster:
schema rejection / CLIENT_CLUSTER_ID_FORBIDDEN

This avoids making a client-controlled value part of the scoring identity.

HD-2 — 23 versus 76 remains unresolved

decision:
NO COMPLETENESS DENOMINATOR SELECTED

The packet-reported quantities may remain descriptive artifact facts.

Neither 23 nor 76 is authorized by this directive as:

the required interview size;

the complete-profile definition;

the lock threshold;

the scientific completeness denominator;

the reveal threshold.

Do not derive one from the other.

HD-3 — remove the current implicit 23-item scientific gate

The present implementation must not use "all currently mapped scoreable question IDs" as an implicitly authorized completeness policy.

Refactor the concepts:

mapped_scoreable_question_ids
= descriptive production-artifact set

completion_required_question_ids
= authority-governed scientific/owner criterion

The second SHALL remain unresolved until owner authorization exists.

Until then:

completionPolicyStatus = UNRESOLVED_OWNER_AUTHORITY

/lock SHALL fail closed with:

SCIENTIFIC_COMPLETENESS_POLICY_UNRESOLVED

and /reveal remains unavailable through that path.

The progress API may expose descriptive facts such as mapped/assessed scoreable counts, but SHALL NOT label an artifact-derived count "required."

While unresolved:

required_confirmatory_question_count should be null/omitted or explicitly unresolved;

adequately_assessed_coverage as a required-completion percentage should be null/omitted;

no owner-facing 100%-complete statement may be derived from the mapped question set.

HD-4 — work allowed before owner completeness authorization

Allowed now:

server-authoritative cluster binding;

removal of client cluster_id;

evidence-quality receipt mechanics;

consistency and minimum-evidence mechanics;

confirmatory/post-hoc phase separation;

invitation/session separation;

credential UX;

private-file and source-version compatibility;

descriptive artifact-count reporting with correct provenance;

completeness-policy abstraction with unresolved state;

fail-closed lock/reveal behavior when completeness authority is absent;

tests, static checks, documentation and durable routing checkpoints for these changes.

Not allowed without new authority:

choosing 23;

choosing 76;

inventing another completeness denominator;

deciding that all mapped dimensions imply completeness;

deciding all non-validation bank records imply completeness;

changing the question bank to force either answer;

changing frozen scientific mappings/scoring merely to resolve this policy question.

HD-5 — pre-repair owner session

decision:
DIAGNOSTIC_ONLY_REPLACEMENT_REQUIRED_FOR_FUTURE_CLEAN_RUN

Preserve the existing pre-repair owner session and its evidence.

Do not delete it and do not retroactively relabel it as conforming to the repaired protocol.

Do not migrate its answers into a new confirmatory session in a way that makes the new run appear prospectively collected.

After:

completeness authority is explicitly resolved,

the intended repaired protocol is reviewed,

deployment is separately authorized,

a fresh session is required for any new owner run intended to exercise that repaired confirmatory protocol.

No replacement session is authorized by this directive.

This is a protocol-integrity decision, not a conclusion about Human Design, astrology, or human scientific validity.

5. AUTHORITY TABLE
Claim ID	Load-bearing claim	Authority/evidence class	State	Consequence
A01	PR #23 head is 7156fd61...	GitHub artifact fact	VERIFIED_CURRENT	Bind HD edits/tests to this head or revalidate if moved
A02	PR #52 head is 6ec73802...	GitHub artifact fact	VERIFIED_CURRENT	Bind MC review to this head or revalidate if moved
A03	Packet reports 81 total / 76 non-validation / 27 rules / 23 mapped IDs	ARTIFACT_DERIVED_FACT evidence	DESCRIPTIVE_ONLY	No scientific criterion follows
A04	23-versus-76 completeness criterion is unresolved	Current owner/request boundary + packets	CONTROLLING	No denominator selection
A05	Claim-authority candidate needs revisions above	REASONING_DECISION	AUTHORITATIVE_FOR_THIS_REVIEW_BOUNDARY	Candidate not canonically adopted unchanged
A06	Worker-controlled labels cannot authenticate reasoning surface	Current owner requirement + reasoning decision	CONTROLLING	Fail such receipts closed
A07	Current Action accepts client cluster_id	GitHub artifact fact	VERIFIED_CURRENT	Remove from client contract
A08	Frozen prediction state contains server-side question/cluster dimensions	GitHub implementation fact	VERIFIED_CURRENT	Resolve cluster server-side
A09	Scientific completeness requires OWNER_EXPLICIT authority in this task	Current user instruction	CONTROLLING	Extra High/Pro may recommend but not bind
A10	Pre-repair session is diagnostic under repaired protocol	REASONING_DECISION based on versioned-protocol incompatibility	CONTROLLING_FOR_NEXT_SLICE	Preserve; fresh run later if authorized
A11	No merge/deploy/invitation rotation/replacement session/contact/spending	OWNER_EXPLICIT	CONTROLLING	Hard prohibition
A12	Both defects require shared Pro meta-review after this response	OWNER_EXPLICIT + both canonical packets	CONTROLLING	Route immediately after external response receipt
A13	Shared Pro lane key below	REASONING_DECISION explicitly requested by owner	AUTHORIZED_ROUTING_DECISION	Supersedes the two defect-specific keys for this combined review
6. ALLOWED MISSION CONTROL EDITS

On PR #52's draft branch only, after the Pro admission sequence below permits implementation:

refine the claim-authority sections already added;

add required-authority/authority-ceiling semantics;

add fact-to-criterion promotion controls;

add reasoning-surface attestation schema/template;

update directive/reconciliation schemas to reference those receipts;

extend hostile fixtures;

extend focused tests;

record this directive and later Pro verdict durably.

Preserve original owner-source blocks and historical defect evidence unchanged.

7. PROHIBITED EDITS / ACTIONS

Do not:

merge either PR;

deploy;

modify Railway production;

rotate invitations;

start the replacement owner session;

contact participants;

spend money;

select 23, 76, or another scientific completeness criterion;

change question-bank scientific content to manufacture a criterion;

alter frozen scoring semantics outside a separately authorized reasoning decision;

infer Human Design/astrology validity;

rewrite immutable owner-source receipts;

convert this response's reasoning into browser/session self-attestation;

claim Pro review before the Pro response receipt exists.

8. REQUIRED TESTS / EVIDENCE
Mission Control

Retain all currently passing claim-authority tests and add at minimum:

artifact fact 76 cannot become scientific criterion without required authority;

artifact fact 23 cannot become scientific criterion without required authority;

reasoning recommendation cannot satisfy OWNER_EXPLICIT;

owner-explicit authorization can satisfy an owner-only criterion;

copying an artifact fact into a contract triggers UNAUTHORIZED_CLAIM_PROMOTION;

a local task called extra_high_* with no eligible surface receipt remains UNVERIFIED;

worker/model self-description "I am Extra High" remains UNVERIFIED;

prompt text requesting Extra High remains UNVERIFIED;

valid pre-submit mode but missing completed-response receipt remains PARTIAL;

session mismatch rejects otherwise valid receipts;

replay of a prior conversation receipt is rejected;

account mismatch is rejected;

all six reasoning-surface receipt classes remain independent;

synthetic evidence cannot satisfy production-evidence requirements;

load-bearing cardinality reproduction never promotes that fact into policy.

Run:

focused Mission Control tests;

full relevant compliance suite;

JSON/schema parse validation;

git diff --check.

Human Design

Add at minimum:

Action schema no longer accepts cluster_id;

attempted client cluster_id injection is rejected;

server resolves cluster from the frozen session dimension;

missing frozen question binding fails closed;

ambiguous frozen question binding fails closed;

persisted scoreable evidence uses server-resolved cluster;

descriptive mapped counts remain available without becoming required counts;

absent owner completeness authority blocks confirmatory lock;

absent authority blocks reveal through the confirmatory path;

neither 23 nor 76 is silently used as completion_required_question_ids;

legacy/pre-repair session remains readable and diagnostic;

protocol/source-version incompatibility prevents silent migration into the repaired run.

Then run:

focused participant/session tests;

full test suite;

strict mypy;

touched-file Ruff;

OpenAPI/schema validation;

participant JS syntax check if touched;

git diff --check.

9. STOP CONDITIONS

Stop substantive execution and return to reasoning if:

either bound PR head has unexpectedly moved before mutation;

server-side question→frozen-dimension resolution is not uniquely defined;

fixing cluster binding would require changing scientific mappings;

any implementation requires selecting a completeness denominator;

an owner-facing field would imply completeness while authority is unresolved;

hidden chart/prediction information would have to be exposed to the Action;

Pro returns REVISE or REJECT on a change that affects the intended Mission Control implementation;

tests reveal a substantive protocol conflict rather than a mechanical bug;

any merge/deployment/session/contact/spending action becomes necessary.

10. COMPLETION CLAIM

For the bounded execution authorized here, the strongest permitted completion claim is:

SUBTASK_COMPLETE_PARENT_OPEN

It requires:

authorized draft edits complete;

required tests/evidence complete;

no prohibited operation performed;

23-versus-76 still explicitly unresolved unless a subsequent owner decision changed it;

Pro meta-review receipt recorded;

remaining parent blockers stated.

It must NOT be upgraded to:

READY_FOR_RELEASE

OWNER_OUTCOME_ACHIEVED

from this directive alone.

11. SHARED PRO SUPERVISION-DESIGN LANE

The two prior keys:

supervision-architecture/a40d413-claim-authority-v1

supervision-architecture/a40d413-reasoning-surface-identity-v1

are reconciled to:

sharedProScopeKey = supervision-architecture/a40d413-authority-provenance-v1

Rationale: both failures are instances of unauthorized authority acquisition caused by provenance/trust-boundary failure. One promotes an untrusted derived value into policy authority; the other promotes an untrusted worker label into reasoning identity authority.

Keep the two defect IDs distinct inside the lane. Do not merge their evidence histories.

12. EXACT PRO PACKET

packetId:
PRO-META-A40D413-AUTHORITY-PROVENANCE-20260901-v1

The transmitted Pro message MUST be self-contained and include, in this order:

The exact CURRENT VERIFIED ROUTING RECEIPT supplied with this review.

Canonical packet 1 in full:
SDF-HUMANDESIGN-76-SCOPE-AUTHORITY-001

Canonical packet 2 in full:
SDF-20260901-REASONING-SURFACE-IDENTITY-LAUNDERING-001

This complete directive:
XH-MC-HD-AUTHORITY-PROVENANCE-20260901 v1.0.0

Current GitHub state:

PR #52 head 6ec73802cf9439be7160f9ac2eea58c7bb95e683

PR #23 head 7156fd61e0e75a56f66024c65357c1256bf8f4f9

Exact relevant implementation excerpts showing:

client Action currently accepts cluster_id;

server frozen prediction dimension owns cluster_id;

current progress contract treats required coverage as lock-relevant;

23-versus-76 remains unresolved.

The externally captured routing receipts for this completed response.

The admission question below verbatim.

Do not substitute GitHub links for the two canonical JSON packets or this directive.

Exact Pro admission question

"Review this as one supervision-design meta-review, not as a Human Design scientific-validity review. Should Mission Control adopt the revised authority-provenance design in XH-MC-HD-AUTHORITY-PROVENANCE-20260901 v1.0.0: (a) explicit required-authority ceilings and logged fact-to-criterion promotion for load-bearing claims, (b) subject-bound provenance and independent reproduction where load-bearing, and (c) session-bound reasoning-surface receipts whose surface, account, visible mode, session, submission and completed response cannot be satisfied by worker-controlled names or self-description? Return ACCEPT, REVISE, or REJECT, with the exact schema/control/test changes required. Do not select 23 or 76, adjudicate Human Design or astrology validity, authorize merge/deployment/session replacement/participant contact/spending, or treat passing fixtures as proof of universal adequacy."

13. PRO-ROUTING RECEIPT

required: true

sharedProScopeKey:
supervision-architecture/a40d413-authority-provenance-v1

packetId:
PRO-META-A40D413-AUTHORITY-PROVENANCE-20260901-v1

currentAdmissionState:
PENDING_EXTERNAL_COMPLETED_RESPONSE_RECEIPT

Reason:
This reasoning response cannot self-attest its conversation/session identity, submitted-message receipt, completed-response receipt, or visible post-response mode.

The supplied pre-submission routing receipt may be carried forward as supplied evidence. The executor must independently add the promised:

conversation URL/session identity;

submitted-message receipt;

completed-response receipt;

visible post-response reasoning mode.

If these bind to this same submitted request/response without contradiction:

nextRoutingState = READY_FOR_PRO_META_REVIEW

Then submit the exact self-contained packet automatically to the shared Pro lane.

If any required routing field is missing or inconsistent:

nextRoutingState = REASONING_SURFACE_RECEIPT_INCOMPLETE

Do not fabricate or substitute a worker-controlled identifier.

The key methodological change is that 23 may remain a production-mapping fact without becoming a completeness rule, just as “Extra High” may remain text in a task name without becoming evidence that an Extra High chat existed. Both require a separate, correctly sourced authority transition. The supplied pre-submission receipt establishes the routing claims you provided, while the conversation/submission/response/post-mode receipts remain intentionally outside this response's self-attestation. 

Pasted text

