# Current state

- Task ID: `full-harness-v1`.
- Goal: complete the reproducible blinded Human Design reverse-matching harness while keeping synthetic engineering validation separate from human validation.
- Branch/worktree: `codex/harness-integration` at `/tmp/hdmatch-integration`; remote draft PR: `https://github.com/u-dont-existDOTcom/humandesign/pull/1`.
- Current published checkpoint: `5604cc0b2d8c09718a54874ba6c385ec3a9f9157`. Both GitHub Actions runs are green (`32524300817` push, `32524305239` pull request). Do not merge the draft PR to `main` yet.
- Owner priority: close every HIGH blind-boundary finding before more research features. Application HIGH findings are closed; claim-grade OS/container isolation remains explicitly `PROCESS-REQUIRED` for each scientific run. After the audit commit and CI are green, return directly to the prospective `MODEL-B-DETAILED-V2-NEW` experiment. Do not resume broad infrastructure work.
- Security checkpoint: synthetic preflight now runs before model loading/cache generation/scoring, scans all bounded decoder-visible public/run/cache/ephemeris paths without filename-extension trust, and exposes no key/reveal recovery surface. The Bubblewrap wrapper provides an allowlisted keyless mount boundary and canonical isolation receipt. A real secret-denial harness and an actual one-case exact recovery both passed under the host boundary. See `docs/16_claim_grade_keyless_recovery.md`.
- Provenance checkpoint: `answer-key-reveal-v2` directly binds the manifest, predictions, freeze, scientific artifacts, exact encrypted envelope, and canonical in-memory decrypted payload. Claim-grade evaluation requires authenticated `RevealResult`; timestamps enforce `manifest <= freeze <= reveal <= evaluation`. Noise comparison verifies each public chain and exact recovery/noise provenance without decryption. Implementation commit: `41826afd2d45a54746adc26b603637d1c108bbd3`.
- Human final-test checkpoint: durable single-use release/cohort/freeze/reveal ledgers passed all requested adversarial cases. Participant-set ordering can no longer bypass the cohort lock. Implementation commit: `aef277c3d640a12fce31e5af2c83c0fc21d1915b`.
- Audit state: `reports/blind_boundary_audit.md` now assigns every HIGH exactly `CLOSED` or `PROCESS-REQUIRED`, with commits, tests, and enforced invariants. This report and this recovery checkpoint are the only intended uncommitted files at this boundary; confirm with `git status --short`.
- Verification after the audit/state edit: complete local suite `260 passed, 2 skipped`; focused security suite `90 passed, 2 skipped`; the two inner-sandbox skips both passed via the host Bubblewrap boundary in 14.07 seconds. Ruff passed; strict mypy passed for 80 source files; task acceptance reports `INITIAL_ENGINEERING_MILESTONE_READY` while explicitly not evaluating the full Model B objective. Both GitHub Actions runs for the published code checkpoint passed; the audit/state commit still must be pushed and checked.
- Completed engineering baseline: the 75-case `MODEL-A-CORE-V1` known-month oracle completed generate → blind recover → freeze → reveal → evaluate. Top-1 `0.453333`, Top-3 `0.746667`, Top-5 `0.826667`, MRR `0.625680`; 41 non-Top-1 cases and all failures/ties remain in `reports/model_a_smoke_75/`. Do not run another expensive Model A benchmark.
- Retained public caches: all 12 months of 2000/UTC, 13,777 stable intervals, 76,416,288 bytes, chart-engine fingerprint `09e811ca0fe517975f9718ea7e12b72f66bf3d2509e049bc29f47169adef5397`. They contain candidate chart states only, no answer keys, and must be preserved.
- Model A remains separately frozen as `MODEL-A-CORE-V1`; mapping SHA-256 `3424672432f7f071ec90ef9ddce52a67ff6794911e92b1a1e04f079262ea6200`.
- Model B V1 remains structural-only/intermediate. It compiles detailed structural families but delegates behavioral questionnaire generation/scoring to Model A because the historical pre-search D1–D10/A1–A6 dictionary did not survive. Never describe it as full behavioral Model B and do not run behavioral A/B while outputs are identical.
- Historical source recovery is concluded. Do not spend more effort reconstructing lost D1–D10/A1–A6 definitions unless a genuinely new provenance-bearing source appears. Result artifacts, winner structures, ranks, and support values may not be used to infer rules. See `mappings/model_b_detailed_v2_source_recovery_audit_v1.json` and `reports/model_b_source_recovery_audit.md`.
- Next scientific task after security closeout: prospectively create and preregister `MODEL-B-DETAILED-V2-NEW`, explicitly not a historical reconstruction. Use only the frozen behavioral target, V3/V4 methodological rules, candidate-blind questionnaire material, and independently sourced standard/primary Human Design meanings from the existing hierarchy. Freeze primary/alternative/corroborating pathways, salience, directness, dependency, genuine contradiction, conditional-prevalence parents, and citations before any benchmark. Then run only a small Model A vs new Model B synthetic comparison, first proving their responses/rankings differ; scale only if discrimination materially improves.
- Verification commands: `/tmp/hdmatch-venv/bin/python -m pytest -q`, `/tmp/hdmatch-venv/bin/ruff check src tests scripts`, `/tmp/hdmatch-venv/bin/mypy src/hdmatch`, and `/tmp/hdmatch-venv/bin/python scripts/task_acceptance.py`.
- Immediate next action: commit the validated audit/state, push `codex/harness-integration`, wait for both GitHub Actions runs to pass, then begin the prospective detailed-model source/mapping work. Do not start the deferred 1,000-case run.
