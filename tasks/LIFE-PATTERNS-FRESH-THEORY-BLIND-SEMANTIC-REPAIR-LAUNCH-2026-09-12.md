# Fresh-context launch — Life Patterns semantic architecture repair — 2026-09-12

Repository: `u-dont-existDOTcom/humandesign`
Branch: `codex/discover-life-patterns-mvp`
Draft PR: `#24`

This launch packet is designed for a **new ChatGPT conversation / fresh worker context** that has not seen target-model mappings, scores, chart outputs, birth-derived predictions, or prior fit results.

GitHub is canonical. Do not rely on any chat summary for implementation state.

## Usable context and launch scope

Use the context-sufficiency section of `docs/research/LIFE_PATTERNS_THEORY_BLIND_CONTENT_AUTHORITY_POLICY.md` before substantive work. Keep the product purpose, longitudinal life context, current task, and acceptance criteria available. Withhold target-model steering information, not the meaning of the job.

This is the existing fresh, selective-reading **Chat reasoning** lane, not an instruction to start a blind semantic task inside a fully loaded Codex checkout. Do not follow the unblinded repository-root reading list or repository-global historical state into scoring/results. Code execution and Git transport may use a separate execution-only worker; they cannot substitute for this semantic role. Preserve actual exposure/provenance rather than claiming that a fresh session alone proves blindness.

## Start exactly here

1. Fetch the live head of PR #24.
2. Read `tasks/ACTIVE-TASK.json`.
3. Read `state/LIFE-PATTERNS-CURRENT-STATE-2026-09-11.md`.
4. Read `docs/research/LIFE_PATTERNS_CODEBOOK_ARCHITECTURE_ROOT_CAUSE_AUDIT_2026-09-11.md`.
5. Read `docs/research/LIFE_PATTERNS_PARTICIPANT_CO_CODING_ARCHITECTURE_2026-09-11.md`.
6. Read `tasks/LIFE-PATTERNS-PARTICIPANT-CO-CODING-SEMANTIC-REPAIR-WORKER-2026-09-11.md`.
7. Follow the worker's remaining required-read order and hard boundaries.

Do **not** read target-model mapping/scoring material, model-fit results, birth/chart outputs, or anything whose purpose is to show which neutral behaviors help or hurt a target theory.

## Task

Execute the semantic-repair worker. The upstream question is:

> What is the **minimum shared frozen substrate actually required** to prevent target-model-specific reinterpretation while preserving a participant-centered Life Patterns product?

Do not assume the historical fixed ontology is necessary merely because it exists.

The repair should preserve useful historical mechanics only where they serve the revised architecture. In particular, maintain exact provenance, missingness semantics, immutable/content-addressed freezes, and strict evidence requirements for genuine absence/nonoccurrence claims.

Do not allow a failed stronger absence claim to erase separately supported positive facts or explicitly narrator-reported appraisals.

Person-level recurring patterns must be recursively participant-adjudicated rather than silently finalized by external coding.

## Required deliverables

Commit all public-safe work to the same branch. Produce only versioned candidate artifacts at this stage:

- disposition of historical mechanics: retain / adapt / supersede;
- minimal revised neutral episode-fact + participant-pattern architecture;
- candidate schema/contract if structured records are needed;
- explicit downstream model-adapter consumption boundary showing how post-hoc model-specific rereading is prevented;
- gap/open-world handling;
- calibration/validation targets appropriate to episode facts and participant-adjudicated patterns;
- focused test requirements;
- compact prompt for a **separate fresh target-theory-blind reviewer**.

Do not implement production code yet.

## Completion condition for this worker

Stop after the candidate artifacts and independent-review prompt are committed. Update public-safe canonical state so that the next action is the separate blind review. Human collection, automated participant coding, target-model scoring/reveal, merge/deploy, recruitment/contact and spending remain unauthorized.
