# Fresh-context launch — Life Patterns semantic architecture review — 2026-09-12

Repository: `u-dont-existDOTcom/humandesign`
Branch: `codex/discover-life-patterns-mvp`
Draft PR: `#24`

Use this only **after** the fresh semantic-repair worker has committed its candidate artifacts and updated canonical state with the exact independent-review prompt/path.

This review must run in a **different new ChatGPT conversation / fresh worker context** from both the supervising conversation and the candidate-producing conversation. The reviewer must not have seen target-model mappings, scores, chart outputs, birth-derived predictions, fit results, or the candidate producer's private reasoning.

## Start exactly here

1. Fetch the live head of PR #24.
2. Read `tasks/ACTIVE-TASK.json`.
3. Read `state/CURRENT-STATE.md` and the dated Life Patterns current-state overlay it names.
4. Confirm that canonical state explicitly names:
   - the candidate semantic/contract artifacts to review;
   - the exact compact independent-review prompt produced by the repair worker;
   - the candidate-producing commit/head.
5. Read and follow that exact independent-review prompt. Do not substitute this launch packet for the candidate-specific review instructions.

If canonical state does not yet identify the candidate and exact review prompt, stop: the candidate-producing worker is not complete.

## Independence boundary

Do not read target-model mapping/scoring material, model-fit results, birth/chart outputs, or any material whose purpose is to reveal which neutral behaviors help or hurt a target theory.

Do not use the candidate producer's rationale except where the candidate-specific review packet explicitly requires a disposition document as review evidence. Form the semantic judgment from the permitted source authority and candidate artifacts.

## Review objective

Determine whether the candidate actually fixes the upstream architecture error rather than merely renaming the fixed codebook. In particular verify, as applicable to the candidate-specific prompt, that:

- one shared frozen substrate exists without presuming a comprehensive closed taxonomy;
- positive episode facts remain preservable independently of stronger absence/nonoccurrence claims;
- narrator-reported appraisals remain attributed reports rather than silently becoming objective conditions;
- genuine absence/nonoccurrence claims retain appropriately strict evidence gates;
- open-world/gap behavior is representable without forced facet certainty;
- person-level recurrence/typicality is participant-adjudicated rather than silently finalized by an external classifier;
- elicited examples are not treated as an unbiased frequency sample;
- exact provenance, uncertainty, counterexamples and participant corrections remain representable;
- downstream model adapters cannot flexibly reread raw narrative after target-model information exists;
- unnecessary ontology/schema complexity has actually been removed rather than shifted to another layer.

## Completion condition

Commit the public-safe independent review/disposition to the same branch and update canonical state with one of:

- `safe_for_implementation=true` with no blocking semantic findings; or
- `safe_for_implementation=false` plus exact blocking findings and the required repair cycle.

Do not implement production code in the review context. Human collection, automated participant coding, target-model scoring/reveal, merge/deploy, recruitment/contact and spending remain unauthorized unless later canonical state explicitly changes those boundaries.
