# Fresh-context blind semantic review — scenario survey v7 repaired candidate

Repository identity: `u-dont-existDOTcom/humandesign`
Candidate branch identity: `chat/scenario-survey-v7-review-repair-20260922`
Candidate commit is recorded in `REVIEW-IDENTITY.txt`.

This is a new independent diagnostic pass. Do not infer or search for prior review findings, producer rationale, repair history, audits, verification verdicts, private participant material, birth/chart data, target-model mappings, fit scores, or prior predictions.

Read only:
1. live default-branch `u-dont-existDOTcom/universal-dev-architecture/AGENTS.md` and only task-triggered universal patterns needed for semantic review;
2. `tasks/ACTIVE-TASK.json` for authority/privacy/runtime boundary only;
3. `tasks/scenario-graph-convergence-20260919/source/source-requirements-v2.json`;
4. `tasks/scenario-survey-v7-redesign-20260922/interviewer-bank-v7.json`;
5. `tasks/scenario-survey-v7-redesign-20260922/INTERVIEW-PROTOCOL-v6.md`;
6. `tasks/scenario-survey-v7-redesign-20260922/EVIDENCE-GUIDE-v7.json`.

No other project files are present in the review workspace. Do not seek them elsewhere.

Goal: judge whether the literal candidate is semantically ready to expose to a fresh respondent. Do not judge whether AstroHD is true.

Audit every canonical route declared by `interviewer-bank-v7.json` `node_count` (currently 79), not a sample. For each route check premise sufficiency, one response task, construct discrimination/information gain, hidden assumptions, redundancy/mirror risk inferable from the bank itself, antecedent admission, context binding, scoped interpretation, compatibility with the source-requirements facet, and whether the protocol suppresses the route when prerequisites are absent.

Inspect the exploratory question only for structural isolation from canonical evidence credit; do not infer a mapping for it.

Return exactly these sections:

BLOCKERS
- numbered; exact node/protocol/evidence-guide location; failure mechanism; why it can materially distort a fresh pilot; minimum semantic repair. Write `none` if there are none.

NONBLOCKING
- numbered useful improvements not required before a fresh pilot. Write `none` if none.

STRONGEST_REMAINING_WEAKNESS
- one concise statement.

AREAS_NOT_EVALUATED_OR_UNDERSTOOD
- explicit list, or `none`.

VERDICT
- exactly `READY_FOR_FRESH_PILOT` or `NOT_READY_FOR_FRESH_PILOT`.

semantic_change_required
- exactly `true` or `false`.

Do not edit files. Freeze findings before ending. Do not perform reconciliation.
