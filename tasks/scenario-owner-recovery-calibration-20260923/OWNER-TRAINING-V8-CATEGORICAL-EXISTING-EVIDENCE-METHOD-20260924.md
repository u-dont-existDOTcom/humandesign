# Owner-trained scenario v8 — existing-evidence categorical bridge — 2026-09-24

Status: **BOUNDED TARGET-AWARE PROJECT DEVELOPMENT; SEMANTIC PASSES CANDIDATE-BLIND; NOT VALIDATION**.

## Why this pass exists

The corrected baseline-observable full-universe scorer does not robustly recover either the recorded date or the recorded time. The three-question target-aware queue is exhausted, so another pairwise owner question is not admitted.

The repository already contains a frozen Survey-v2 categorical/adaptive architecture for Profile and planetary Gate fields. The original scenario-v7 interview predates its typed planetary evidence-custody format, so existing answers cannot enter those categorical jobs without a candidate-blind evidence-routing bridge.

This pass tests the materially simpler alternative: **reuse existing answers before collecting anything else**.

## Source boundary

Use only the original 79-answer target-blind measurement snapshot. Exclude the later target-aware v8 Q1–Q3 answers from this categorical pass.

Private source: the frozen v2 measurement whose public SHA-256 is already recorded in `tasks/ACTIVE-TASK.json`.

No raw participant text or participant-specific categorical output may enter public Git.

## Stage A — evidence routing only

Run in a fresh isolated subscription-model context.

Inputs allowed:
- exact question/answer text and stable private event IDs from the 79-answer snapshot;
- frozen Survey-v2 domain-role definitions and required evidence categories for Moon, Mercury, Venus, and Profile;
- Personality versus Design custody rule.

Inputs forbidden:
- Gate/Profile archetype vocabulary;
- birth/date/time/place;
- chart features;
- predicted labels;
- candidate identities;
- scores/ranks;
- target or competitor information;
- prior categorical interpretations.

Task: select only verbatim evidence that genuinely fits a required category. Preserve uncertainty and context. A unit may not be copied across Personality and Design custody. Missing categories stay missing; do not manufacture a complete job.

Stage-A output is evidence routing, not classification and not scoreable by itself.

## Stage B — frozen categorical classification

Run in a different fresh isolated subscription-model context.

For each routed job, supply:
- only that job's sealed evidence units with immutable category/custody tags;
- the complete frozen vocabulary for that field;
- the frozen field role, minimum-evidence rule, confidence rubric, and classifier instructions.

Use the existing Survey-v2 classifier contract and system prompt. Candidate-blind prohibitions remain exact. The classifier returns every equally supported eligible label; inadequate evidence abstains.

Do not repair, retry semantically, or add evidence after seeing a categorical result. Serialization/transport failure may follow the frozen technical-failure rules only.

## Initial field set

Attempt only the existing baseline categorical fields before any adaptive expansion:
- `personality:moon`
- `design:moon`
- `personality:mercury`
- `design:mercury`
- `personality:venus`
- `design:venus`
- `profile`

Do not pre-classify the 15 adaptive planetary fields merely because they exist.

## Stage C — score and adaptive decision

Append only admitted categorical classifications to the existing full-universe partial-evidence score. Preserve dependency-family normalization and abstentions. Structural rarity remains prohibited.

Rescore the complete verified century cache.

If the owner target is not robustly recovered, the next action is determined only by the frozen anonymous adaptive selector over the current candidate partition and unanswered eligible fields. The selector may not see participant prose, true-candidate identity, birth metadata, score direction relative to the known target, or owner-selected desired fields.

A further owner question is admitted only if:
1. existing evidence cannot satisfy the selected field;
2. the anonymous selector shows material expected information gain;
3. exact user-facing wording passes the seven rendered-question admission checks;
4. the question is frozen before the owner answer.

## Stop/efficacy rule

If no baseline categorical field can be validly classified from existing evidence, record that result rather than weakening evidence gates.

If admitted categorical evidence does not materially improve full-universe recovery, classify this strategy cycle as flat/negative and replace the method rather than repeatedly mining the same transcript.

All outputs remain owner-training/post-selection development evidence.
