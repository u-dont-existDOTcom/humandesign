# Life Patterns v2 implementation launch

Work only on `codex/discover-life-patterns-mvp` / draft PR #24.

Fetch live head, run `python scripts/task_preflight.py`, and follow `tasks/ACTIVE-TASK.json`.

Implementation authority is the frozen v2 candidate, contract, focused test matrix, and PASS review already named by canonical state. Implement them as a new code path alongside historical fixed-codebook code; do not redesign or mutate accepted semantic artifacts.

Create the deterministic typed record/validation/freeze/projection implementation and focused unit tests for T001-T006. Full projection validation must require theory-blind semantic callbacks for real-world nonoccurrence routing and current-proposition grounding; structural labels alone are not a bypass.

Preserve append-only fact lineage, immutable evidence timing, fact qualification provenance, participant pattern authority, episode-only fact projection, deterministic hashing, open-world facts, and the adapter firewall.

Use UDA test-efficiency telemetry. Run focused tests, affected tests, then one handoff-checkpoint unit suite plus lint/typecheck. Do not edit canonical task/state; return execution evidence to the supervising Chat.
