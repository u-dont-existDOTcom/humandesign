# Pro supervision checkpoint 2 — 2026-08-30

## Ruling

`OWNER DECISION REQUIRED: NO`

The deterministic foundation is architecturally accepted but not yet scientifically closed. The checkpoint packet showed that evidence lineage, weekday locking, candidate-date handling, separation from relationship data, immutable records, synthetic-only public export, privacy gates, and the prohibition on inferential semantics satisfy the approved architecture.

The remaining blocking gap is actual-engine conformance. The generic interval machinery and its coverage receipts were qualified with `SyntheticAnalyticEphemerisProvider`; they do not yet prove candidate-complete transition enumeration for the repository's canonical Human Design/AstroHD engine.

## Authorized bounded slice

Continue on `codex/astrohd-relationship-continuation` with **real-engine deterministic conformance only**, using conspicuously synthetic birth records. The authorized work is:

1. identify and pin the one canonical repository chart engine, or stop and return to Pro if the repository admits materially incompatible candidates;
2. build or complete its adapter to the interval enumerator;
3. establish the actual temporal input resolution, rounding/equality behavior, Julian-date conversion behavior, and design-root precision;
4. generate a complete machine-readable inventory of every emitted or derivable engine field, with identity/exclusion classifications and reasons;
5. prove why the transition candidate family is exhaustive for every included discrete field, including direct/retrograde/repeated crossings, stationary behavior, design-side changes, near-coincident changes, and day-edge equality;
6. generate real-engine synthetic coverage receipts for ordinary, leap, DST gap/fold, non-one-hour DST, historical offset, skipped-date, non-integer-offset, rapid-transition, multi-date, reduced-signature-collision, near-coincident, and design-root-tolerance cases;
7. add an independent conformance check that does not merely run the same interval code twice;
8. produce a machine-readable matrix for every checkpoint-1 evidence transition;
9. conduct a bounded literature/methods scan as documentation only, beginning from an independent conception snapshot;
10. return to Pro before designing or implementing any ranking, questionnaire, calibration, candidate-selection, abstention, stopping, or participant-facing semantics.

## Required real-engine identity packet

The packet must identify the engine implementation, source/package version or source digest, mandala/bodygraph constants digests, ephemeris implementation and data checksums, design-root solver and tolerance, runtime digest, dependency-lock digest, and timezone database version/digest.

Every actual engine field must be classified as one of:

- discrete state identity;
- candidate coordinate;
- continuous diagnostic;
- provenance;
- unsupported or unavailable;
- excluded from identity with a precise reason.

If the engine emits color, tone, base, variable, node, or other advanced-substructure values, a generic availability/status flag is not sufficient. A future decision to include a previously excluded discrete field requires a new identity version and complete re-enumeration.

"Exact" may be claimed only relative to demonstrated engine precision. Microsecond-formatted boundaries must not imply microsecond scientific precision if the actual engine distinguishes only coarser instants.

## Required evidence-state matrix

The machine-readable matrix must cover documentary and memory dates with unavailable, concordant, and conflicting weekday evidence; conflicting documentary sources; explicit candidate sets; correction/supersession; attempted omission of the original declared date; relationship-evidence injection; and attempted in-place mutation. Each case records assessment state, enumeration permission, operative dates, and evidence-lineage digest.

## Literature-scan boundary

The scan may document established work in birth-time rectification, uncertain-input and interval-censored inference, prior sensitivity, calibration, remembered-temporal-fact reliability, data leakage/double dipping, nested participant-level validation, selective/post-selection inference, abstention, conformal/set-valued prediction, questionnaire reliability, and privacy-preserving outcome ledgers.

It must distinguish directly reusable methods, methods needing adaptation, incompatible methods, unresolved questions, and the strongest established baseline for any bespoke component. It may not select weights, priors, formulas, thresholds, items, or participant-facing labels.

## Hard stops

Stop and return to Pro if the canonical engine cannot be identified or pinned, incompatible engines require a substantive choice, actual resolution remains unknown, complete transition generation cannot be justified, arbitrary time buckets are needed, a changing discrete field would be omitted, advanced-substructure values are replaced by status only, distinct full states would merge, or exactness would exceed demonstrated precision.

Also stop for any relationship-assisted pruning, rank/weight/prior/probability/confidence/duration-mass/stopping/recommendation semantics, questionnaire implementation, participant-record access, public natal mounting, production migration, Railway/variable changes, unproven `OPENROUTER_API_KEY` removal, private-data leakage, or reuse of stale PR #1.

## Operational boundary

Local continuation commits are authorized on the current task branch. Pro stated that a feature-branch push could occur only after exact-head privacy/secret gates; repository owner policy still requires Joel's direct push authority, so no push is performed here.

Merge and deployment remain prohibited. Do not merge or rebase `main`, deploy Railway, mount the natal API publicly, migrate production, edit variables, access participant records, publish participant-level natal artifacts, claim real-engine qualification before the next Pro review, or begin inferential semantics. Any future merge consideration additionally requires successful real-engine review and effective GitHub exact-head branch protection/ruleset enforcement.
