# Pro supervision checkpoint 3 — 2026-08-30

## Ruling

`OWNER DECISION REQUIRED: NO`

`Deterministic-foundation verdict: QUALIFIED`

ChatGPT Pro qualified the deterministic foundation from the supplied checkpoint packet; it did not independently inspect the repository. The qualifying packet described clean local head `8cc97025f1e30c52a156e3c7bb5068baf5aea39b`, unchanged GitHub/Railway state, exact-head verification, the V4 engine identity, all real-engine fixtures, the evidence matrix, the bounded methods scan, and the disclosed aggregate-replay wrapper timeout.

## Qualified claim

Given a manifest-valid explicit candidate-date/place/timezone domain, the pinned repository engine deterministically partitions each valid local civil day into maximal half-open intervals whose complete declared discrete chart state is constant. It reports candidate-complete stable/variable set facts relative to the pinned engine, ephemeris, constants, timezone database, runtime, floating-point behavior, and state-identity specification.

The qualification rests on:

- the unambiguous fail-closed canonical engine and local ephemeris;
- the complete runtime field inventory and explicit exclusions;
- the distinction between the one-microsecond Python coordinate grid and the coarser effective binary64 Julian grid;
- first-changed-coordinate boundary semantics without an astronomical microsecond claim;
- transition construction from every primitive activation leaf used by derived Bodygraph state;
- the structurally independent enumerator;
- exact production/independent event agreement across design-side, repeated, coincident, near-coincident, reduced-signature-collision, and day-edge cases;
- ordinary, leap, DST gap/fold, non-one-hour DST, historical/fractional offset, multi-date, and nonexistent-date coverage;
- the complete evidence transition matrix;
- exact-head tests, static analysis, privacy gates, and deterministic artifact checks.

The qualification does not establish Human Design or astrological validity, birth-time recoverability from self-description, human reliability or construct validity, any ranking/weight/prior/likelihood/probability/confidence/stopping semantics, relationship inference, participant-facing usefulness, production readiness, sufficiency of the 4,807 reduced signatures, irrelevance of unavailable advanced fields, or authorization to push, merge, migrate, or deploy.

## Qualification invalidation boundary

Return to conformance review if any of these change:

- chart-engine source or provider behavior;
- ephemeris files/settings;
- constants;
- runtime or materially relevant dependency versions;
- timezone database;
- floating-point conversion or design-root semantics;
- boundary-search implementation;
- state canonicalization or identity membership;
- a currently unavailable discrete leaf becomes available;
- a currently excluded discrete field is used in natal or relationship analysis.

Operation outside the manifest-pinned engine and ephemeris support domain remains fail closed and is not qualified.

## Aggregate replay ruling

The detached aggregate replay did not require scientific remediation because no assertion, digest, interval partition, implementation, artifact, or production/independent equality check failed. The fixture artifact had already been generated from unchanged implementation commit `320d5af`; six groups were revalidated during the aggregate rerun, Kathmandu separately recomputed to the committed 42-interval result digest, and the exact-head conformance tests exercised the transition and boundary/tolerance checks.

The monolithic replay remains operationally fragile. Checkpoint 4 requires reproducibility hardening:

1. Replay is fixture-granular and resumable.
2. Each fixture receipt binds the exact repository commit, engine-identity packet digest, fixture-input digest, civil-day-domain digest, ordered-interval-list digest, coverage-receipt digest, result digest, and independent-verification status.
3. Receipts cover all eight successful synthetic civil days and the Pacific/Apia fail-closed case.
4. Every successful fixture exactly matches the committed interval count and result digest.
5. The aggregate index is calculated from completed per-fixture receipt hashes without recomputing every transition certificate.
6. Interrupted execution resumes from verified receipts and never treats incomplete work as success.
7. Missing, duplicated, stale-head, wrong-engine, or mismatched receipts fail the aggregate check.
8. The existing immutable fixture artifact remains unchanged; a new exact-head replay index may reference it and attest matching component digests.
9. Hardening may not alter the qualified enumerator, adapter, identity, evidence state machine, or result semantics. Any such alteration reopens conformance.

## Next authorized slice

The exact scope is **pre-inference study-design, falsification, and disclosure contract only**. It may produce a frozen protocol and synthetic-only validation infrastructure. It may not implement participant inference. Local commits are authorized on `codex/astrohd-relationship-continuation`; push is not authorized.

### Evaluation unit and reference standard

The contract must distinguish:

- `C_i`: participant `i`'s complete candidate interval set, frozen before inferential response evidence;
- `T_i`: an independently sourced documentary reference-time interval, withheld from development and inference;
- `S_i`: a future procedure's returned subset of `C_i`, or explicit abstention.

Documentary precision is preserved as an interval. A record rounded to five minutes, fifteen minutes, or an hour is not promoted to an exact minute. Memory-only times may be studied for feasibility/reliability but are not accuracy ground truth. Participants without an independent reference interval are excluded from calibration/validation claims about recovery.

Candidate-construction date/weekday/source evidence stays separate from hidden `T_i`. Reference leakage into candidate construction, questionnaire generation, model fitting, or stopping invalidates that participant for validation.

### Non-scalar estimand

The primary target is a joint coverage–set-width–abstention frontier, not a correct minute, raw rank, or weighted utility scalar. For every untouched validation participant, report separately:

- whether `S_i` intersects `T_i`;
- temporal width retained in `S_i` relative to `C_i`;
- full-state interval count retained relative to `C_i`;
- whether the procedure abstained;
- date coverage when `C_i` spans dates.

Temporal width and state count remain separate. Abstention is neither success nor error. This slice may not select an operating threshold, coverage target, width target, or preferred frontier point.

### Required baselines

Predeclare at least:

- complete unordered candidate set;
- no pruning after responses;
- random/permuted subsets matched separately on temporal width and full-state count;
- calendar, season, birthplace, timezone, cohort, and source-quality controls without HD state;
- response-style-only controls;
- participant-to-chart label permutation;
- plausible mismatched-chart and blinded-matching controls;
- the strongest ordinary non-HD model supportable by the same admissible inputs.

A future HD method must show incremental out-of-sample performance over the strongest applicable non-HD baseline. Beating random assignment alone is insufficient. Random-width and random-state-count controls are not probability models.

### Data roles and leakage

Define disjoint development, calibration, and locked-validation roles. Every adaptive choice belongs to development, including concepts/wording, features, coding, missingness, model family, priors, hyperparameters, thresholds, abstention, baselines, subgroup definitions, and outcome transformations.

Calibration may calibrate only a frozen method and may not become repeated development. Validation remains inaccessible until the method and analysis plan are frozen. If validation affects methodology, that cohort becomes development and a new untouched cohort is required.

Group splits by participant, repeated identity, relationship/household connected component, and shared record source. A person, partner, household member, connected relationship pair, or label-transmitting shared source may not cross roles. Relationship evidence remains excluded; any later separately approved relationship-assisted exploration permanently disqualifies that relationship and connected participants from validation.

### Measurement-development requirements

Define future requirements without writing questions, choices, scoring keys, or chart-linked interpretations. Cover test-retest and inter-rater reliability, missingness, acquiescence/response style, social desirability, Forer/Barnum susceptibility, item transparency/chart-feature cueing, construct overlap, language/population invariance, blinded authorship/evaluation where practical, and separation of item generation from held-out labels. Any item-writing phase requires another Pro checkpoint.

### Feasibility analysis

Reproducible synthetic calculations may vary explicitly hypothetical effect sizes and prevalence assumptions to estimate sample requirements or precision ranges for coverage, width reduction, abstention, paired baseline comparison, potential future calibration error, and subgroup/source-quality sensitivity.

Do not select a final cohort size, claim an expected HD effect, treat synthetic-state counts as human sample size, use synthetic oracle accuracy as a human effect estimate, or commit the owner to recruitment, cost, or expenditure.

### Aggregate public-ledger threat model

Design but do not deploy an aggregate public-safe outcome ledger. Address exact birth linkage, sparse state fingerprints, membership inference, rare candidate sets, repeated-release differencing, relationship-network linkage, deterministic personal-data hashes, small cells, free text, withdrawal/deletion, and versioned corrections without public personal linkage.

The default ledger is cohort-aggregate only. Participant-level natal rows, chart intervals, exact dates/places, source documents, relationship identifiers, and personal-data hashes remain prohibited. Do not finalize a suppression threshold, privacy budget, or release policy in this slice.

### Methods decision ledger

Classify every reviewed method family as direct reuse, adaptation, baseline only, incompatible, or unresolved/experimental; state the reason and the evidence required before use. Do not select an estimator. Cover interval-censored reference data, prior sensitivity, calibration, abstention/rejection, conformal/set-valued approaches, measurement reliability, participant/connected-component splitting, nested adaptation, permutation/negative controls, selective/post-selection inference, and disclosure control.

## Checkpoint 4 package

Return to Pro with:

- exact branch head/diff and a content-hashed design contract;
- formal definitions of `C_i`, `T_i`, `S_i`, coverage, temporal width, state count, and abstention;
- reference-source eligibility and precision rules;
- data-role access and contamination rules;
- connected-component splits and synthetic leakage tests;
- complete baseline/falsification matrix;
- measurement requirements and proof no items were written;
- reproducible feasibility sensitivities with every assumption visible;
- aggregate-ledger threat model and synthetic aggregate schema;
- methods decision ledger;
- fixture-granular replay index satisfying the hardening rules;
- machine-readable unresolved-decision register;
- exact-head tests, typing/lint, privacy gates, and clean worktree;
- confirmation that all qualified deterministic components remained unchanged.

## Explicit prohibitions

Still prohibited: push or PR changes; merge/rebase/cherry-pick/force-update involving `main`; GitHub governance changes; stale PR #1; Railway/data/config/variable/deployment changes; reading secret values; removing `OPENROUTER_API_KEY`; live records; recruitment; questionnaire content or scoring; candidate ranking/pruning/elimination; priors/weights/scores/probabilities/confidence/duration mass; numerical stopping/abstention thresholds; recommended dates/windows/times; participant-facing natal output; relationship evidence/inference; public-ledger implementation/publication; HD-validity, rectification-accuracy, or human-calibration claims; treating unavailable fields as irrelevant; treating reduced signatures as sufficient; or modifying qualified deterministic components without reopening conformance.

## Owner-decision boundary

No owner decision is required for this reversible, synthetic-only, non-participant-facing slice. A later owner decision is required before questionnaire development, a participant-facing use case, an operating point on the coverage/width/abstention frontier, a human cohort size/burden/cost, candidate ranking/exclusion, probabilistic language, public-ledger deployment, push, merge, migration, or deployment.
